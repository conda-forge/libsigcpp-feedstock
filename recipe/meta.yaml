{% set major_minor = "2.10" %}
{% set version = major_minor + ".3" %}
{% set abi_version = "2.0" %}

package:
  name: libsigcpp
  version: {{ version }}

source:
  fn: libsigc++-{{ version }}.tar.xz
  url: http://ftp.acc.umu.se/pub/GNOME/sources/libsigc++/{{ major_minor }}/libsigc++-{{ version }}.tar.xz
  sha256: f843d6346260bfcb4426259e314512b99e296e8ca241d771d21ac64f28298d81

build:
  number: 0
  skip: true  # [win]
  run_exports:
    - libsigcpp-{{ abi_version }} >={{ version }}

outputs:
  - name: libsigcpp-{{ abi_version }}
    script: build_libsigcpp.sh
    build:
      run_exports:
        - libsigcpp-{{ abi_version }} >={{ version }}
    requirements:
      build:
        - autoconf
        - automake
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
      host:
        - m4
      run_constrained:
        - libsigcpp {{ version }}
    test:
      commands:
        - test -f ${PREFIX}/lib/libsigc-{{ abi_version }}${SHLIB_EXT}

  - name: libsigcpp
    requirements:
      run:
        - libsigcpp-{{ abi_version }}  {{ version }}
    test:
      commands:
        - test -f ${PREFIX}/lib/pkg-config/sigc++-{{ abi_version }}.pc

about:
  home: http://libsigc.sourceforge.net/
  license: LGPL-2.1-or-later
  license_file: COPYING
  summary: "libsigc++ implements a typesafe callback system for standard C++."

extra:
  recipe-maintainers:
    - scopatz
    - isuruf
